enum RepeatInterval {
  daily,
  weekly,
  monthly,
  yearly,
}

enum DoTime {
  morning,
  afternoon,
  evening,
  custom,
}


enum RepeatFrequency {
  daily,
  weekly,
  monthly,
}


enum RepeatType {
  daily,
  weekly,
  monthly,
  yearly,
  custom,
}
